#!/usr/bin/env bash
if [[ $# -ne 1 ]]; then
    echo "Usage: $0 <process_name>"
    exit 1
fi

PROC=$1
if pgrep -x $PROC > /dev/null; then
    echo "1" > "/tmp/$PROC-was-running" 
    pkill $PROC
else
    echo "0" > "/tmp/$PROC-was-running" 
fi


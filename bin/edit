#!/usr/bin/env bash

if [[ $# -ne 1 ]]; then
    echo "Usage: $0 /path/to/edit"
fi

PATH_TO_EDIT=$1
if [[ -d "$PATH_TO_EDIT" ]]; then
    DIR="$PATH_TO_EDIT"
else
    DIR=$(dirname "$PATH_TO_EDIT")
fi
cd "$DIR"
nvim "$PATH_TO_EDIT"
